{
  "name": "ai-editor-desktop",
  "productName": "AI Editor",
  "version": "0.0.42",
  "license": "MIT",
  "description": "AI Editor 桌面版（Electron 外壳）",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "pnpm run build && electron .",
    "test": "vitest run",
    "dist": "pnpm run build && node scripts/pack.mjs"
  },
  "dependencies": {
    "@whispering233/ai-editor-server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "electron": "44.3.0",
    "electron-builder": "^26.15.3",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
