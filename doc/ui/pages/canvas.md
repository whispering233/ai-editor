# Canvas 画布原型（已废弃，2026-08 决策 33）

> **废弃声明（批次八 O6，2026-08）**：本章原型对应的「画布」页面已按用户裁决删除（决策 33）——1000 章后画布难以显示，删除中栏 tab 并清理全部相关代码（`pages/Canvas.tsx`、`lib/canvas.ts` 及测试、TabBar/路由接线）。
>
> 保留不变的部分：**`plot_edge` 关系类型与关系接口能力**（决策 10 数据模型，可经 `POST/GET/DELETE /relation` 读写，仅无 UI 入口）；旧 localStorage 坐标（`ai-editor:canvas:{project_id}`）为无害残留，不清理。
>
> 本文档保留仅为追溯原型设计历史，**不再作为实现契约**。路由/布局契约以 `layout.md`（已移除 `#/canvas`）为准。

---

## 原型设计（历史追溯，原内容摘要）

- 路由：`#/canvas`
- 定位：大纲节点画布投影（决策 1：同一数据的两种投影），坐标/缩放存 localStorage（决策 10）
- 交互原设计：节点拖拽/缩放、plot_edge 连线创建与删除、拖出即连、连线标签线上编辑、小地图、一键重排、hover 路径高亮（S10.1-S10.5 / UX1）